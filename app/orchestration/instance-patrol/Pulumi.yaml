name: instance-patrol
description: Auto-terminate instances with an expiration tag
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
